neo4j
anthropic>=0.103.0
pygit2
python-dotenv
pydantic
pydantic-settings
fastapi
uvicorn[standard]
rich

# Qwen3 embedding models need transformers>=4.51.0, but transformers 5.x
# registers custom Torch ops that are not compatible with the pinned CPU
# torch 2.4 image used by Dockerfile.repograph.
sentence-transformers==4.1.0
transformers==4.51.3
huggingface-hub>=0.30.0,<1.0

## Stage 2 (AST overlay via CGC)
codegraphcontext
kuzu

## Stage 3 (optional LlamaIndex semantic layer) — install when starting GraphRAG
# llama-index-core==0.10.55
# llama-index-graph-stores-neo4j==0.2.7
# llama-index-vector-stores-qdrant==0.2.13
# llama-index-embeddings-voyageai==0.1.4
# llama-index-llms-anthropic==0.1.16
# qdrant-client==1.10.1
# voyageai==0.2.3
